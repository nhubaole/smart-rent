import 'package:flutter/material.dart';

class RoomInArea extends StatelessWidget {
  const RoomInArea({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 200,
    );
  }
}
